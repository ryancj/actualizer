<div class="features features-4">
  <div class="container">
    <div class="row">
      <div class="col-xl-12">

        <div class="features-4-content">
          <h2>
            <%= get_content("#{component}.header") %>
          </h2>
          <div class="row">
            <div class="col-xl-4 col-xl-4 col-lg-4 feature">
              <h6><%= get_content("#{component}.service_one_header") %></h6>
              <p>
                <%= get_content("#{component}.service_one_content") %>
              </p>
            </div>
            <div class="col-xl-4 col-xl-4 col-lg-4 feature">
              <h6><%= get_content("#{component}.service_two_header") %></h6>
              <p>
                 <%= get_content("#{component}.service_two_content") %>
              </p>
            </div>
            <div class="col-xl-4 col-xl-4 col-lg-4 feature last">
              <h6><%= get_content("#{component}.service_three_header") %></h6>
              <p>
                <%= get_content("#{component}.service_three_content") %>
              </p>
            </div>
          </div>
          <a href="#">
            <button type="button" class="btn btn-primary"><%= get_content("#{component}.cta") %></button>
          </a>
        </div>

      </div>
    </div>
  </div>
</div>
